defaults:
  - task: example   # Placeholder task config; fill in dataset- and task-specific fields.
  - policy: act
  - _self_

seed: 1
device: cuda
batch_size: 1        # Small batches keep debugging easy during inference.
num_workers: 0
checkpoint_path: ??? # Required checkpoint path.
stats_path: null     # Optional normalization stats path.
split: val           # train OR val
num_batches: 1       # Stop early for quick inspection runs.
output_path: null    # Optional output file for serialized predictions.

hydra:
  run:
    dir: outputs/inference/${now:%Y-%m-%d}/${now:%H-%M-%S}
